//! microdot-browser — browser-tab adapter for microdot-core.
//!
//! Placeholder. The full WebSocket bridge, `BrowserPlatform`,
//! `LocalStorageProvider`, and `BrowserConnect` land in a follow-up.
//! This crate exists in the workspace now so the layout is in place
//! and `microdot-core` can be built and tested without breakage.

#![cfg_attr(not(test), allow(dead_code))]
